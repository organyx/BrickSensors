<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aleks.bricksensors" >

    <uses-feature android:name="android.hardware.screen.portrait" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher2"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CompassActivity"
            android:label="@string/title_activity_compass"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".LightSaberActivity"
            android:label="@string/title_activity_light_saber"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".GeigerCounterActivity"
            android:label="@string/title_activity_geiger_counter"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>
